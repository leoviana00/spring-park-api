# JPA / HIBERNATE
# Mostrar ou não log para cada consulta sql
spring.jpa.show-sql=true 
# Mostra instruçoes SQL nos logs, formatado(true). Em uma unica linha (false)
spring.jpa.properties.hibernate.format_sql=true